#!/usr/bin/python3
""" method outside a class"""


def is_same_class(obj, a_class):
    """methof to check whether obj is an instance of a_class"""
    if type(obj) is a_class:
        return (True)
    else:
        return (False)
